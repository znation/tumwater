# tumwater is public: resolve from the public npm registry, not a machine-local mirror, so
# package-lock.json never records a private registry URL. This overrides ~/.npmrc.
registry=https://registry.npmjs.org/
